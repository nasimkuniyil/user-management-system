
interface IUserData {
    name: string;
    email: string;
    password: string;
}

const validateForm: (userData: IUserData) => void = ({ name, email, password }) => {
    try {

    } catch (error) {
        //error code
    }
}


export default validateForm;